node_modules/
dist/
.next/
.env
.env.local
.env.example
state/journal/*.jsonl
state/logs/*.json
state/logs/*.jsonl
*.log
.DS_Store
coverage/

# next
apps/web/.next/
apps/web/next-env.d.ts
apps/web/tsconfig.tsbuildinfo

# The engineering record is PRIVATE and stays that way: docs/ (PRD, whitepaper,
# interfaces, RFCs, spikes, runbook), state/STATE.md, prompt.md, and the
# submission drafts. README.md is the only public document, so it has to stand
# on its own — do not add a link from it into anything below.
docs/
*.md
!README.md
